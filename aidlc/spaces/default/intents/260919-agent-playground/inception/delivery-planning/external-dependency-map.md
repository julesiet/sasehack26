# External Dependency Map — Text/HTTP Agent Playground

Empty — fully AI-contained. The only "dependency" is the already-existing,
already-running `apps/api` process in the same repo (not an external team,
API, or approval gate); it requires no coordination outside this build.
