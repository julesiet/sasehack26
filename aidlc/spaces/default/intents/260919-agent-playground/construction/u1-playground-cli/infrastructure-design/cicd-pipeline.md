None new for this unit. CI/linter adoption is a separate, general project
decision (practices-discovery), explicitly out of scope for this specific
issue per `requirements.md`'s Out of Scope section. This unit's tests run
via the existing `pnpm test`, same as every other package.
