# Infrastructure Specification — U1

None. Deployment is explicitly out of scope for this issue (confirmed in
Requirements Analysis Q5 and Delivery Planning's external-dependency-map).
The CLI runs from an existing local dev checkout via `pnpm playground`; no
new infrastructure, environment, or hosting target is provisioned.
