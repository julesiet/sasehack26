# Entities — U1: PlaygroundCli

```yaml
entities: []
```

No new entities. PlaygroundCli formats data already defined by the
existing system's contracts (`packages/shared/src` — tool results, policy
decisions, conversation-turn responses); it introduces no new domain
concepts, per domain-design's confirmed decision.
