# Scalability Requirements — U1: PlaygroundCli

Not applicable. A manually-run, single-invocation local CLI has no
concurrency or scale dimension — one process, one request, one exit. No
scaling target is set.
