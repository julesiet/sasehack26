# Simulator: localhost is fine.
# Expo Go on a phone: use this Mac's LAN IP so the QR is not localhost.
#   ipconfig getifaddr en0
# EXPO_PUBLIC_API_URL=http://192.168.1.12:3001
EXPO_PUBLIC_API_URL=http://localhost:3001
